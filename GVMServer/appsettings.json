{
  "ConfiguredHotUpdates": true, // 配置热更新
  "Web": {
    "Prefixes": [ "http://*:8802/" ], // 侦听的载荷协议及域，本服务器不建议设定SSL（不然要搞x509），但你可以在NGINX之中配置SSL
    "ConcurrentConnections": {
      "MinHandlingWorkerCount": 32, // 最小处理工作者数量
      "MinCompletionPortCount": 32, // 最小完成端口数量
      "MaxHandlingWorkerCount": 64, // 最大处理工作者数量（此值不意味着越大越好，越大所需要的CPU资源则越多）。
      "MaxCompletionPortCount": 64 // 最大完成端口数量（此值不意味着越大越好，越大所需要的CPU资源则越多）。
    },
    "GetIPAddressFromHttpHeaders": false // 从HTTP协议上获取IP地址
  },
  "ExecutionCharacterSet": "utf-8", // 执行字符集（execution_character_set）指定为UTF-8
  "Logging": {
    "Root": "./log"
  },
  "Gateway": {
    "ListenPort": 8803 // SDK网关侦听的端口
  },
  "PayControl": { // 支付控制
    "IgnoreSignatureVerification": true, // 忽略签名验证（调试时启用）
    "PayNotifyRawUri": "http://47.112.124.168:8802/api/pay/notify"
  },
  "GMControl": { // GM控制
    "RawUri": "/api/gm",
    "AppSercrt": "SdDextwdmwXd33LpYEWzazDDpLS300XX",
    "GMSercrtKey": "tyXMYvE178mOXN7Oy9E7WmVyM",
    "AppID": "102008388913",
    "ChannelId": 25,
    "GameChannelId": 2699,
    "IgnoreTimeoutVerification": true, // 忽略超时验证（调试时启用）
    "IgnoreSignatureVerification": true, // 忽略签名验证（调试时启用）
    "TimestampErrorPrecision": 300, // 时间戳误差精度
    "InterruptTimeAckAvailableInNSeconds": 30, // 中断时在N秒内ACK可用
    "InternalFirewall": {
      "AllowOrigin": 2, // -1：阻止任何主机的访问，0：允许任何组合或客户端访问，1：只允许局域网内访问，2：只允许环路访问
      "ExceptionHosts": [ "118.122.92.34", "118.31.236.188", "114.55.225.227", "182.150.20.30", "193.168.1.217" ] // 例外的主机
    },
    "Error": {
      "InterfaceIsUndefined": {
        "Code": 403,
        "Message": "接口未定义",
        "Tag": {} // 负载数据
      },
      "InvalidRequestArgument": { // 无效参数
        "Code": 412,
        "Message": "参数不合法，请对照文档检查参数",
        "Tag": {} // 负载数据
      },
      "CanNotCallInNonAck": { // 不能再投递ACK
        "Code": 500,
        "Message": "游戏内部错误，无法向游戏服务器发起远过程调用",
        "Tag": {} // 负载数据
      },
      "HttpMethodNotAvailable": { // HTTP请求方法不可用
        "Code": 504,
        "Message": "HTTP请求方法不可用",
        "Tag": {} // 负载数据
      },
      "GMWebProtocolHeaderError": { // GMWeb协议报文头部错误
        "Code": 444,
        "Message": "GMWeb协议报文头部错误",
        "Tag": {} // 负载数据
      },
      "TimespanIsTimeout": {
        "Code": 408,
        "Message": "接口时间戳（timestamp）超时,timestamp有效时间为5分钟内，超时需要重新生成，需重新生成请求",
        "Tag": {} // 负载数据
      },
      "InvalidSignatureText": {
        "Code": 401,
        "Message": "signature签名错误",
        "Tag": {} // 负载数据
      },
      "UnableToResolveParameter": {
        "Code": 412,
        "Message": "参数不合法，提供的参数并不是一个空的字符串或者BSON/JSON数据",
        "Tag": {} // 负载数据
      },
      "ArgumentOutOfLimiting": {
        "Code": 446,
        "Message": "参数溢出限制",
        "Tag": {}
      },
      "RetryAckTimeout": {
        "Code": 503,
        "Message": "服务不可用。服务器目前无法使用（由于超载或停机维护）。通常，这只是暂时状态。",
        "Tag": {} // 负载数据
      },
      "AckCorrectButNotResponse": {
        "Code": 10001,
        "Message": "基础框架发现一个未知不被许可的错误，状态显示成功但并没有响应正文",
        "Tag": {} // 负载数据
      },
      "SeriousLinklineLayerFailure": {
        "Code": 502,
        "Message": "网关错误，基础框架向游戏服务器往返重试ACK的过程中，存在链路故障",
        "Tag": {} // 负载数据
      },
      "UnableToPassThroughFirewall": {
        "Code": 402,
        "Message": "请求IP受限，非法请求IP，请求被拒绝",
        "Tag": {} // 负载数据
      }
    },
    "Interfaces": {
      "empty": { // 接口路由（必须是小写，不允许带首位空）
        "name": "empty", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "timestamp": {
            "type": "number",
            "size": 10,
            "nullable": true
          }
        }
      },
      "chat_forbidden": { // 接口路由（必须是小写，不允许带首位空）
        "name": "chat_forbidden", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "user_id": {
            "type": "string", // 数据类型（string、number、boolean、guid、ip、json、jtoken、jarray、jobject、jvalue）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "type": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "expire_time": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "gm_add": { // 接口路由（必须是小写，不允许带首位空）
        "name": "gm_add", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "gm_list": { // 接口路由（必须是小写，不允许带首位空）
        "name": "gm_list", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "sort": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page": {
            "type": "number", // 数据类型（number）
            "size": 5, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page_size": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "gm_delete": { // 接口路由（必须是小写，不允许带首位空）
        "name": "gm_delete", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "guild": { // 接口路由（必须是小写，不允许带首位空）
        "name": "guild", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "sort": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page": {
            "type": "number", // 数据类型（number）
            "size": 5, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page_size": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "guild_dismiss": { // 接口路由（必须是小写，不允许带首位空）
        "name": "guild_dismiss", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "guild_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "guild_member": { // 接口路由（必须是小写，不允许带首位空）
        "name": "guild_member", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "guild_id": {
            "type": "number", // 数据类型（number）
            "size": 20, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "sort": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page": {
            "type": "number", // 数据类型（number）
            "size": 5, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page_size": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "server_hot": { // 接口路由（必须是小写，不允许带首位空）
        "name": "server_hot", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "server_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "kick": { // 接口路由（必须是小写，不允许带首位空）
        "name": "kick", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "level_up": { // 接口路由（必须是小写，不允许带首位空）
        "name": "level_up", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "level": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "isauto": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "role_list": { // 接口路由（必须是小写，不允许带首位空）
        "name": "role_list", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "sort": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page": {
            "type": "number", // 数据类型（number）
            "size": 5, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "page_size": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "role_id": {
            "type": "string", // 数据类型（number）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": true // 可空（否则必须填写参数）
          },
          "role_name": {
            "type": "string", // 数据类型（number）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": true // 可空（否则必须填写参数）
          },
          "guild_id": {
            "type": "string", // 数据类型（number）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": true // 可空（否则必须填写参数）
          },
          "ip": {
            "type": "string", // 数据类型（number）
            "size": 16, // 最大长度（ <=0 不限制长度）
            "nullable": true // 可空（否则必须填写参数）
          },
          "is_online": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": true // 可空（否则必须填写参数）
          }
        }
      },
      "recharge": { // 接口路由（必须是小写，不允许带首位空）
        "name": "recharge", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "gold": {
            "type": "number", // 数据类型（number）
            "size": 4, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "role_detail": { // 接口路由（必须是小写，不允许带首位空）
        "name": "role_detail", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "type": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "role_query": { // 接口路由（必须是小写，不允许带首位空）
        "name": "role_query", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "send_chat": { // 接口路由（必须是小写，不允许带首位空）
        "name": "send_chat", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "content": {
            "type": "string", // 数据类型（string）
            "size": 200, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "send_mail": { // 接口路由（必须是小写，不允许带首位空）
        "name": "send_mail", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "type": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "title": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "content": {
            "type": "string", // 数据类型（string）
            "size": 200, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "notice_send": { // 接口路由（必须是小写，不允许带首位空）
        "name": "notice_send", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "id": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "type": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "title": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "content": {
            "type": "string", // 数据类型（string）
            "size": 200, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "start_time": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "end_time": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "interval": {
            "type": "number", // 数据类型（number）
            "size": 5, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "notice_delete": { // 接口路由（必须是小写，不允许带首位空）
        "name": "notice_delete", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "id": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "hot_update": { // 接口路由（必须是小写，不允许带首位空）
        "name": "hot_update", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "type": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "server_update": { // 接口路由（必须是小写，不允许带首位空）
        "name": "server_update", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "server_name": {
            "type": "string", // 数据类型（string）
            "size": 15, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "close_time": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "open_time": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "server_reset": { // 接口路由（必须是小写，不允许带首位空）
        "name": "server_reset", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "reset_time": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "kick_all": { // 接口路由（必须是小写，不允许带首位空）
        "name": "kick_all", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "type": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "send_mail_all": { // 接口路由（必须是小写，不允许带首位空）
        "name": "send_mail_all", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "send_type": {
            "type": "number", // 数据类型（string）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "type": {
            "type": "number", // 数据类型（number）
            "size": 10, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "title": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "content": {
            "type": "string", // 数据类型（string）
            "size": 200, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "query_limit": { // 接口路由（必须是小写，不允许带首位空）
        "name": "query_limit", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "type": {
            "type": "number", // 数据类型（number）
            "size": 1, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "test_mail": { // 接口路由（必须是小写，不允许带首位空）
        "name": "test_mail", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "role_id": {
            "type": "string", // 数据类型（string）
            "size": 30, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          },
          "mail_id": {
            "type": "number", // 数据类型（number）
            "size": 3, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "server_status": { // 接口路由（必须是小写，不允许带首位空）
        "name": "server_status", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "status": {
            "type": "number", // 数据类型（number）
            "size": 3, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "interface": { // 接口路由（必须是小写，不允许带首位空）
        "name": "interface", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": { // 映射请求模型
          "type": {
            "type": "number", // 数据类型（number）
            "size": 3, // 最大长度（ <=0 不限制长度）
            "nullable": false // 可空（否则必须填写参数）
          }
        }
      },
      "set_pos": { // 接口路由（必须是小写，不允许带首位空）
        "name": "set_pos", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "pos_x": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "pos_y": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "map_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SkillEffect": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SkillEffect", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "skill_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "skill_effect_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "GmMonster": { // 接口路由（必须是小写，不允许带首位空）
        "name": "GmMonster", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "monster_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "count": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "GmClickItem": { // 接口路由（必须是小写，不允许带首位空）
        "name": "GmClickItem", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "click_item_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddItem": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddItem", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "item_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "count": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "quality": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "bind": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "HP": { // 接口路由（必须是小写，不允许带首位空）
        "name": "HP", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "CopperCoin": { // 接口路由（必须是小写，不允许带首位空）
        "name": "CopperCoin", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddExp": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddExp", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "Drop": { // 接口路由（必须是小写，不允许带首位空）
        "name": "Drop", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "CleanBackPack": { // 接口路由（必须是小写，不允许带首位空）
        "name": "CleanBackPack", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SetLevel": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SetLevel", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "ChgCareer": { // 接口路由（必须是小写，不允许带首位空）
        "name": "ChgCareer", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "career_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "Reborn": { // 接口路由（必须是小写，不允许带首位空）
        "name": "Reborn", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "KickOne": { // 接口路由（必须是小写，不允许带首位空）
        "name": "KickOne", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "TestMail": { // 接口路由（必须是小写，不允许带首位空）
        "name": "TestMail", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "mail_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "ResetPartner": { // 接口路由（必须是小写，不允许带首位空）
        "name": "ResetPartner", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "CleanPartner": { // 接口路由（必须是小写，不允许带首位空）
        "name": "CleanPartner", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "TestShop": { // 接口路由（必须是小写，不允许带首位空）
        "name": "TestShop", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "shop_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "ShopBuy": { // 接口路由（必须是小写，不允许带首位空）
        "name": "ShopBuy", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "shop_uid": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "shop_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "shop_item_index": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "shop_item_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "count": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "CleanBuyLimited": { // 接口路由（必须是小写，不允许带首位空）
        "name": "CleanBuyLimited", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "TimeOffset": { // 接口路由（必须是小写，不允许带首位空）
        "name": "TimeOffset", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "offset": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "TestRandTask": { // 接口路由（必须是小写，不允许带首位空）
        "name": "TestRandTask", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "rand_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "CleanRandTask": { // 接口路由（必须是小写，不允许带首位空）
        "name": "CleanRandTask", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "VipLevel": { // 接口路由（必须是小写，不允许带首位空）
        "name": "VipLevel", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "time": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "exp": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "Trick": { // 接口路由（必须是小写，不允许带首位空）
        "name": "Trick", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "command": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "trick_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SkillCmd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SkillCmd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "command": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "career_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "skill_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SetFlagCmd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SetFlagCmd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "index": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SwichMapCfgIdCmd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SwichMapCfgIdCmd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "map_cfg_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "EventCmd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "EventCmd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "event_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "RankBonusCmd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "RankBonusCmd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "rank_type": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "check_clear_rank_data": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "RankClearCmd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "RankClearCmd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "rank_type": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "NServerClose": { // 接口路由（必须是小写，不允许带首位空）
        "name": "NServerClose", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "hour": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "minute": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "TestNotice": { // 接口路由（必须是小写，不允许带首位空）
        "name": "TestNotice", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddTask": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddTask", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "task_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "DoneTask": { // 接口路由（必须是小写，不允许带首位空）
        "name": "DoneTask", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "task_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "PushMessageCommand": { // 接口路由（必须是小写，不允许带首位空）
        "name": "PushMessageCommand", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "title": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "context": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "param": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddSpecialState": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddSpecialState", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "life_time": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SetArenaRank": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SetArenaRank", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "rank": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "GmSave": { // 接口路由（必须是小写，不允许带首位空）
        "name": "GmSave", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "unique_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "GmWatchAdd": { // 接口路由（必须是小写，不允许带首位空）
        "name": "GmWatchAdd", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "unique_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "GmWatchDel": { // 接口路由（必须是小写，不允许带首位空）
        "name": "GmWatchDel", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "unique_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddFashion": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddFashion", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "cfg_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "Draw": { // 接口路由（必须是小写，不允许带首位空）
        "name": "Draw", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SetStat": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SetStat", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "command": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "value": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddCareer": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddCareer", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "career_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddTitle": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddTitle", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "title": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "AddDailyRechargeAmount": { // 接口路由（必须是小写，不允许带首位空）
        "name": "AddDailyRechargeAmount", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "amount": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "ReceivedDailyRechargeAmount": { // 接口路由（必须是小写，不允许带首位空）
        "name": "ReceivedDailyRechargeAmount", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "type": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "position": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SetFirstTimePayTopUpMask": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SetFirstTimePayTopUpMask", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "SetDailyRewardMicroTimeOffset": { // 接口路由（必须是小写，不允许带首位空）
        "name": "SetDailyRewardMicroTimeOffset", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "RechargeAckPrizeAwardEmulator": { // 接口路由（必须是小写，不允许带首位空）
        "name": "RechargeAckPrizeAwardEmulator", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "DrawGizmos": { // 接口路由（必须是小写，不允许带首位空）
        "name": "DrawGizmos", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "mask": {
            "type": "string",
            "size": 30,
            "nullable": false
          }
        }
      },
      "client_control": { // 接口路由（必须是小写，不允许带首位空）
        "name": "client_control", // 接口名称
        "method": "POST", // 请求协议
        "ignore": true, // 忽略未知参数
        "request": { // 映射请求模型
          "role_id": {
            "type": "string",
            "size": 30,
            "nullable": false
          },
          "control": {
            "type": "string",
            "size": 1024,
            "nullable": false
          }
        }
      },
      "log_control": { // 接口路由（必须是小写，不允许带首位空）
        "name": "log_control", // 接口名称
        "method": "POST", // 请求协议
        "ignore": false, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": null
      },
      "gm_exts": { // 接口路由（必须是小写，不允许带首位空）
        "name": "gm_exts", // 接口名称
        "method": "POST", // 请求协议
        "ignore": false, // 忽略未知参数（若启用则请求模型未指定的属性都将被抛弃，它讲显著的降低流量同时提高安全性）
        "request": null
      }
    }
  },
  "Sdk": {
    "Dockings": [
      {
        "PackageName": "com.qld.jhlkx",
        "AppId": "106448802467",
        "AppKey": "8fcb114296db7edaa9445f185be7176b",
        "AppSecret": "SdDextwdmwXd33LpYEWzazDDpLS300XX",
        "SignToLower": false
      },
      {
        "PackageName": "com.qld.dxll.xiyoui",
        "AppId": "102858234828",
        "AppKey": "16b1b52fc302a0c9c7de9bdd597f08a9",
        "AppSecret": "pu1VVUru1nFk1vjf1R7lfxXp9FzJXpNn",
        "SignToLower": false
      }
    ]
  }
}